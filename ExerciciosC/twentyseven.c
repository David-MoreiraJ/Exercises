#include <stdio.h>
#include <string.h>

int main(void)
{
    int n = 0;
    int p;
    char = c;

    printf("Qual o valor maximo que o computador pode ler?");
    scanf("%d", n);

    printf("Qual opera��o voc� quer realizar e com qual valor?? Op��es + ou *");
    scanf("%d %c", p , c);

    if(p c p >= n) {
        printf("OVERFLOW");
    }
    else {
        printf("OK");

        return 0;
}
    
