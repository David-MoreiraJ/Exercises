#include <stdio.h>
#include <math.h>

int main (void) {

    int x;
    double n;

   printf("Quantas raizes voce quer calcular? \n");
   scanf("%d", &x);

   while ( x > 0 ) {
       printf("Me diga uma raiz! \n");
       scanf("%lf", n);
       double sqrt (double x);
       printf("lf", n);
       x--;
   }

}
